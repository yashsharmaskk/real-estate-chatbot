name: Keep Render Backend Awake

on:
  schedule:
    # This cron schedule runs the script every 10 minutes
    - cron: '*/10 * * * *'
  workflow_dispatch: # Allows you to trigger this manually from the GitHub UI if needed

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Health Endpoint
        # REPLACE THE URL BELOW with your actual Render backend URL
        run: curl -s https://real-estate-chatbot-8fdj.onrender.com/health